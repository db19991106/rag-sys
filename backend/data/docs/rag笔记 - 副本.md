1.  1.1 登录页面
     现有按钮：
      - ✅ 登录按钮
      - ✅ 重置按钮
      - ✅ 返回登录页按钮（成功页面）

     评估： 功能完整，满足基本认证需求。

   ---

     1.2 知识文档管理页面
     现有按钮：
      - ✅ 上传文档按钮
      - ✅ 重命名按钮（每文档）
      - ✅ 重新处理按钮
      - ✅ 删除按钮

     缺失功能：
      - ❌ 批量导入文档
      - ❌ 文档预览功能
      - ❌ 文档分类/标签管理
      - ❌ 批量删除文档
      - ❌ 文档版本管理
      - ❌ 文档内容编辑
      - ❌ 导入进度显示
      - ❌ OCR识别配置（图片/PDF）
      - ❌ 文档清洗配置
      - ❌ 多种数据源导入（数据库、API等）

     完整性评估： 40% - 仅支持基本的单文件上传和简单管理

   ---

     1.3 文档切分页面
     现有按钮：
      - ✅ 执行切分按钮
      - ✅ 重置配置按钮
      - ✅ 保存切分结果按钮
      - ✅ 全选/批量删除按钮
      - ✅ 批量合并按钮
      - ✅ 单个片段操作（拆分、合并、删除）

     缺失功能：
      - ❌ 切分模板保存/加载
      - ❌ 批量切分配置
      - ❌ 切分质量评估
      - ❌ 片段预览对比
      - ❌ 智能切分建议
      - ❌ 切分历史记录
      - ❌ 导出切分配置
      - ❌ 批量应用切分策略
      - ❌ 切分结果统计分析
      - ❌ 语义边界检测

     完整性评估： 60% - 核心功能具备，但缺少高级配置和优化功能

   ---

     1.4 向量索引页面
     现有按钮：
      - ✅ 批量生成向量按钮
      - ✅ 刷新状态按钮
      - ✅ 2D/3D分布切换
      - ✅ 单个片段重新生成向量
      - ✅ 查看向量详情

     缺失功能：
      - ❌ 向量模型选择/配置
      - ❌ 向量维度调整
      - ❌ 向量索引类型选择（HNSW/IVF/PQ等）
      - ❌ 索引参数调优
      - ❌ 向量相似度阈值配置
      - ❌ 向量数据库连接配置
      - ❌ 向量备份/恢复
      - ❌ 向量质量分析
      - ❌ 向量聚类分析
      - ❌ 索引重建策略选择

     完整性评估： 50% - 支持基础向量化，但缺少高级配置和向量库管理功能

   ---

     1.5 查询检索页面
     现有按钮：
      - ✅ 执行检索按钮
      - ✅ 清空结果按钮
      - ✅ 查看详情按钮

     缺失功能：
      - ❌ 检索模型选择
      - ❌ 检索算法配置（余弦/欧氏/点积）
      - ❌ 重排序配置（Reranker）
      - ❌ 检索结果过滤
      - ❌ 检索历史记录
      - ❌ 检索结果导出
      - ❌ 检索性能监控
      - ❌ 批量查询
      - ❌ 查询优化建议
      - ❌ 检索A/B测试

     完整性评估： 40% - 基础检索功能具备，但缺少高级配置和分析功能

   ---

     1.6 上下文构建页面
     现有按钮：
      - ✅ 重置上下文按钮
      - ✅ 刷新Prompt按钮
      - ✅ 构建生成上下文按钮
      - ✅ 清空所有片段按钮
      - ✅ 删除按钮

     缺失功能：
      - ❌ Prompt模板管理
      - ❌ Prompt版本控制
      - ❌ 动态Prompt配置
      - ❌ Prompt测试/验证
      - ❌ Prompt效果对比
      - ❌ 上下文长度限制配置
      - ❌ 智能片段排序
      - ❌ 相关性过滤
      - ❌ 多轮对话上下文
      - ❌ Prompt导出/导入

  三、功能补充方案

  3.1 数据导入/处理相关按钮

  3.1.1 文档批量导入
   按钮位置：知识文档管理页面顶部工具栏
   按钮标签：批量导入文档
   操作逻辑：
   1. 点击后弹出批量导入配置对话框
   2. 支持选择文件夹或多个文件
   3. 显示导入进度条和实时状态
   4. 支持暂停/继续/取消导入
   5. 导入完成后显示统计信息（成功/失败/跳过）
      RAG流程位置：文档加载阶段

  3.1.2 文档分类管理
   按钮位置：文档列表上方工具栏
   按钮标签：文档分类
   操作逻辑：
   1. 打开分类管理面板
   2. 创建/编辑/删除分类
   3. 支持标签管理
   4. 支持按分类筛选文档
   5. 支持批量设置分类
      RAG流程位置：文档组织阶段

  3.1.3 文档预览
   按钮位置：每个文档行的操作列
   按钮标签：预览
   操作逻辑：
   1. 点击后在侧边或弹窗显示文档内容
   2. 支持滚动浏览
   3. 显示文档元数据（大小、页数、创建时间等）
   4. 支持下载查看
      RAG流程位置：文档查看阶段

  3.1.4 文档内容编辑
   按钮位置：文档操作菜单
   按钮标签：编辑内容
   操作逻辑：
   1. 打开文档编辑器
   2. 支持富文本编辑
   3. 支持版本保存和对比
   4. 支持撤销/重做
   5. 编辑后自动更新文档状态
      RAG流程位置：文档预处理阶段

  3.1.5 OCR识别配置
   按钮位置：文档导入设置
   按钮标签：OCR设置
   操作逻辑：
   1. 打开OCR配置面板
   2. 选择OCR引擎（Tesseract/百度/阿里云等）
   3. 配置识别语言和精度
   4. 配置表格识别
   5. 测试OCR效果
      RAG流程位置：非结构化数据提取阶段

  3.1.6 数据源导入
   按钮位置：文档管理页面的"导入"下拉菜单
   按钮标签：从数据库导入 / 从API导入
   操作逻辑：
   1. 数据库导入：配置数据库连接 → 选择表/字段 → 预览数据 → 导入
   2. API导入：配置API地址和参数 → 测试连接 → 导入
   3. 支持定时导入配置
   4. 支持数据增量更新
      RAG流程位置：数据源接入阶段

---

  3.2 文档分块与嵌入配置按钮

  3.2.1 切分模板保存/加载
   按钮位置：切分配置区域
   按钮标签：保存模板 / 加载模板
   操作逻辑：
   1. 保存模板：输入模板名称 → 保存当前切分配置
   2. 加载模板：选择模板 → 应用配置
   3. 支持模板管理（编辑/删除/导出）
   4. 支持模板预览
      RAG流程位置：文档切分配置阶段

  3.2.2 切分质量评估
   按钮位置：切分结果区域
   按钮标签：质量评估
   操作逻辑：
   1. 点击后运行质量评估算法
   2. 显示评估指标（语义完整性、长度分布、重叠率等）
   3. 标注低质量片段
   4. 提供优化建议
   5. 支持一键优化
      RAG流程位置：切分结果优化阶段

  3.2.3 智能切分建议
   按钮位置：切分配置区域
   按钮标签：智能建议
   操作逻辑：
   1. 分析文档内容结构
   2. 根据内容特征推荐切分策略
   3. 推荐最优参数
   4. 显示推荐理由
   5. 支持一键应用推荐
      RAG流程位置：切分策略优化阶段

  3.2.4 批量切分
   按钮位置：文档列表页
   按钮标签：批量切分
   操作逻辑：
   1. 选择多个文档
   2. 应用统一或个性化切分配置
   3. 显示切分进度
   4. 支持暂停/继续
   5. 完成后显示批量处理报告
      RAG流程位置：批量文档处理阶段

  3.2.5 向量模型配置
   按钮位置：向量索引页面顶部
   按钮标签：模型配置
   操作逻辑：
   1. 打开模型配置面板
   2. 选择向量模型（BGE/text2vec/ernie等）
   3. 配置模型参数（维度、batch_size等）
   4. 测试模型性能
   5. 支持模型切换和比较
      RAG流程位置：向量化模型选择阶段

  3.2.6 向量维度配置
   按钮位置：向量生成设置
   按钮标签：维度设置
   操作逻辑：
   1. 选择向量维度（256/512/768/1024/1536）
   2. 显示不同维度的性能对比
   3. 预估存储和检索开销
   4. 支持自定义维度
      RAG流程位置：向量空间配置阶段

---

  3.3 向量数据库管理按钮

  3.3.1 向量数据库连接配置
   按钮位置：向量索引页面顶部
   按钮标签：数据库连接
   操作逻辑：
   1. 打开连接配置面板
   2. 选择数据库类型（FAISS/Milvus/Pinecone/Qdrant等）
   3. 配置连接参数
   4. 测试连接
   5. 支持多数据库切换
      RAG流程位置：向量库接入阶段

  3.3.2 索引类型选择
   按钮位置：向量管理设置
   按钮标签：索引配置
   操作逻辑：
   1. 打开索引配置面板
   2. 选择索引类型（HNSW/IVF/PQ/IVF_PQ等）
   3. 配置索引参数（ef_construction/nlist等）
   4. 显示性能预估
   5. 支持索引对比测试
      RAG流程位置：向量索引构建阶段

  3.3.3 向量备份/恢复
   按钮位置：向量数据库操作菜单
   按钮标签：备份向量 / 恢复向量
   操作逻辑：
   1. 备份：选择备份范围 → 执行备份 → 显示备份文件
   2. 恢复：选择备份文件 → 预览 → 执行恢复
   3. 支持定时备份配置
   4. 支持增量备份
      RAG流程位置：数据安全管理阶段

  3.3.4 向量质量分析
   按钮位置：向量管理页面
   按钮标签：质量分析
   操作逻辑：
   1. 分析向量分布特征
   2. 检测异常向量
   3. 显示质量指标
   4. 提供清理建议
   5. 支持一键清理
      RAG流程位置：向量数据维护阶段

  3.3.5 索引重建策略
   按钮位置：向量管理操作
   按钮标签：重建索引
   操作逻辑：
   1. 选择重建策略（全量/增量）
   2. 配置重建参数
   3. 显示重建进度
   4. 支持后台重建
   5. 完成后显示统计信息
      RAG流程位置：索引优化阶段

---

  3.4 检索策略调整按钮

  3.4.1 检索算法配置
   按钮位置：检索配置区域
   按钮标签：算法配置
   操作逻辑：
   1. 打开算法配置面板
   2. 选择相似度算法（余弦/欧氏/点积）
   3. 配置算法参数
   4. 显示算法性能对比
   5. 支持算法切换
      RAG流程位置：检索算法选择阶段

  3.4.2 重排序配置
   按钮位置：检索配置区域
   按钮标签：重排序设置
   操作逻辑：
   1. 打开重排序配置面板
   2. 选择重排序模型（Cohere/BGE-Reranker等）
   3. 配置重排序参数
   4. 支持重排序开关
   5. 测试重排序效果
      RAG流程位置：检索结果优化阶段

  3.4.3 检索结果过滤
   按钮位置：检索结果区域
   按钮标签：结果过滤
   操作逻辑：
   1. 打开过滤配置面板
   2. 设置过滤条件（日期、分类、标签等）
   3. 配置相似度范围
   4. 实时应用过滤
   5. 支持保存过滤模板
      RAG流程位置：检索结果筛选阶段

  3.4.4 检索性能监控
   按钮位置：检索页面顶部
   按钮标签：性能监控
   操作逻辑：
   1. 打开性能监控面板
   2. 显示检索延迟、吞吐量等指标
   3. 显示检索成功率
   4. 支持性能预警配置
   5. 导出性能报告
      RAG流程位置：检索性能优化阶段

  3.4.5 检索A/B测试
   按钮位置：检索配置区域
   按钮标签：A/B测试
   操作逻辑：
   1. 创建测试计划
   2. 配置不同检索策略
   3. 分配流量比例
   4. 收集测试数据
   5. 显示测试报告和对比结果
      RAG流程位置：检索策略优化阶段

---

  3.5 生成参数配置按钮

  3.5.1 LLM模型配置
   按钮位置：生成配置区域
   按钮标签：模型配置
   操作逻辑：
   1. 打开LLM配置面板
   2. 选择模型（GPT/ChatGLM/Qwen等）
   3. 配置API密钥和参数
   4. 测试模型连接
   5. 支持多模型切换
      RAG流程位置：生成模型选择阶段

  3.5.2 生成参数调优
   按钮位置：生成配置区域
   按钮标签：参数调优
   操作逻辑：
   1. 打开参数配置面板
   2. 配置temperature、top_p、max_tokens等
   3. 显示参数说明和建议值
   4. 支持参数模板
   5. 测试参数效果
      RAG流程位置：生成优化阶段

  3.5.3 Prompt模板管理
   按钮位置：上下文构建页面
   按钮标签：模板管理
   操作逻辑：
   1. 打开模板管理面板
   2. 创建/编辑/删除模板
   3. 支持模板版本控制
   4. 支持模板导入/导出
   5. 支持模板A/B测试
      RAG流程位置：Prompt工程阶段

  3.5.4 Prompt测试验证
   按钮位置：Prompt预览区域
   按钮标签：测试Prompt
   操作逻辑：
   1. 选择测试Prompt
   2. 输入测试查询
   3. 调用LLM生成结果
   4. 显示生成结果和评价
   5. 支持批量测试
      RAG流程位置：Prompt优化阶段

  3.5.5 上下文长度优化
   按钮位置：上下文构建设置
   按钮标签：长度优化
   操作逻辑：
   1. 分析当前上下文长度
   2. 显示长度分布和建议
   3. 自动调整片段数量
   4. 支持最大长度限制配置
   5. 实时预览优化效果
      RAG流程位置：上下文优化阶段

---

  3.6 流程步骤可视化控制按钮

  3.6.1 流程步骤控制
   按钮位置：流程可视化区域
   按钮标签：步骤控制
   操作逻辑：
   1. 显示完整RAG流程步骤
   2. 支持单步执行
   3. 支持跳过某步骤
   4. 支持回退到某步骤
   5. 显示每步骤状态和耗时
      RAG流程位置：全流程控制阶段

  3.6.2 流程参数全局配置
   按钮位置：页面顶部全局设置
   按钮标签：全局配置
   操作逻辑：
   1. 打开全局配置面板
   2. 配置所有流程步骤参数
   3. 支持参数预设模板
   4. 支持参数导入/导出
   5. 显示配置历史版本
      RAG流程位置：流程参数管理阶段

  3.6.3 流程执行监控
   按钮位置：流程可视化区域
   按钮标签：执行监控
   操作逻辑：
   1. 实时显示流程执行状态
   2. 显示每步骤进度
   3. 显示错误和警告
   4. 支持暂停/继续/停止
   5. 导出执行日志
      RAG流程位置：流程监控阶段

  3.6.4 流程性能分析
   按钮位置：流程分析页面
   按钮标签：性能分析
   操作逻辑：
   1. 分析每个步骤的性能指标
   2. 识别性能瓶颈
   3. 显示优化建议
   4. 支持性能对比
   5. 导出分析报告
      RAG流程位置：流程优化阶段

  3.6.5 流程自动化配置
   按钮位置：流程设置
   按钮标签：自动化配置
   操作逻辑：
   1. 配置自动化触发条件
   2. 配置自动化执行流程
   3. 设置定时任务
   4. 配置事件触发
   5. 监控自动化执行状态
      RAG流程位置：流程自动化阶段

---

  3.7 历史记录查看按钮

  3.7.1 操作历史记录
   按钮位置：侧边栏"历史记录"
   按钮标签：操作历史
   操作逻辑：
   1. 显示所有操作历史
   2. 按时间/类型/用户筛选
   3. 显示操作详情和参数
   4. 支持回滚操作
   5. 导出历史记录
      RAG流程位置：操作审计阶段

  3.7.2 查询历史记录
   按钮位置：检索页面
   按钮标签：查询历史
   操作逻辑：
   1. 显示历史查询记录
   2. 支持重复历史查询
   3. 显示查询结果快照
   4. 支持历史对比
   5. 导出查询历史
      RAG流程位置：查询复现阶段

  3.7.3 生成历史记录
   按钮位置：生成页面
   按钮标签：生成历史
   操作逻辑：
   1. 显示历史生成记录
   2. 显示输入Prompt和输出结果
   3. 支持重新生成
   4. 支持结果对比
   5. 导出生成历史
      RAG流程位置：生成回顾阶段

  3.7.4 配置历史版本
   按钮位置：全局设置
   按钮标签：版本历史
   操作逻辑：
   1. 显示配置变更历史
   2. 支持版本对比
   3. 支持版本回滚
   4. 支持版本标记
   5. 导出版本历史
      RAG流程位置：配置管理阶段

  3.7.5 性能历史趋势
   按钮位置：性能监控页面
   按钮标签：历史趋势
   操作逻辑：
   1. 显示性能历史趋势图
   2. 支持时间范围选择
   3. 显示关键指标变化
   4. 识别性能异常
   5. 导出趋势报告
      RAG流程位置：性能追踪阶段

---

  3.8 结果导出按钮

  3.8.1 导出检索结果
   按钮位置：检索结果区域
   按钮标签：导出结果
   操作逻辑：
   1. 选择导出格式（JSON/CSV/Excel）
   2. 选择导出字段
   3. 配置导出参数
   4. 下载导出文件
   5. 支持定时导出配置
      RAG流程位置：结果输出阶段

  3.8.2 导出生成结果
   按钮位置：生成结果区域
   按钮标签：导出回答
   操作逻辑：
   1. 选择导出格式（TXT/HTML/PDF）
   2. 选择导出模板
   3. 预览导出内容
   4. 下载导出文件
   5. 支持批量导出
      RAG流程位置：结果输出阶段

  3.8.3 导出向量数据
   按钮位置：向量管理页面
   按钮标签：导出向量
   操作逻辑：
   1. 选择导出范围
   2. 选择导出格式（FAISS/NPY/JSON）
   3. 配置导出参数
   4. 显示导出进度
   5. 下载导出文件
      RAG流程位置：数据备份阶段

  3.8.4 导出配置文件
   按钮位置：全局设置
   按钮标签：导出配置
   操作逻辑：
   1. 选择导出配置范围
   2. 配置导出格式
   3. 预览导出内容
   4. 下载配置文件
   5. 支持配置分享
      RAG流程位置：配置备份阶段

  3.8.5 导出分析报告
   按钮位置：分析页面
   按钮标签：导出报告
   操作逻辑：
   1. 选择报告类型
   2. 配置报告内容
   3. 选择报告格式
   4. 生成报告预览
   5. 下载报告文件
      RAG流程位置：分析输出阶段

